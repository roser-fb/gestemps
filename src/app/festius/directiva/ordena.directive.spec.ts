import { OrdenaDirective } from './ordena.directive';

describe('OrdenaDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdenaDirective();
    expect(directive).toBeTruthy();
  });
});
