import { NewpasswordPipe } from './newpassword.pipe';

describe('NewpasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new NewpasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
