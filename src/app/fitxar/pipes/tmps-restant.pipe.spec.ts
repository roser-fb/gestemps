import { TmpsRestantPipe } from './tmps-restant.pipe';

describe('TmpsRestantPipe', () => {
  it('create an instance', () => {
    const pipe = new TmpsRestantPipe();
    expect(pipe).toBeTruthy();
  });
});
